# Copy to .env and set your token
TELEGRAM_BOT_TOKEN=123456:ABC-your-telegram-bot-token
# Optional: maximum addresses to fetch per source (default 200)
MAX_PER_SOURCE=200
# Optional: HTTP timeout seconds
HTTP_TIMEOUT=20
